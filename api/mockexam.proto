syntax = "proto3";

option go_package = "api/DISYS_MockExam";

service IncrementService {
    rpc Increment(IncrementMsg) returns (Response);
}

message IncrementMsg {

}

message Response {
    int32 value = 1;
}
